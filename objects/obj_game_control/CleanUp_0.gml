for (var i = 0; i < array_length(todos_blocos); i++) {
	delete todos_blocos[i];
}

for (var j = 0; j < array_length(array_ruas); j++) {
	delete array_ruas[j];
}

for (var j = 0; j < array_length(array_arvs); j++) {
	delete array_arvs[j];
}

